# Python
scripts/.venv/
scripts/.venv-nima/
.venv/
scripts/__pycache__/
*.pyc

# NIMA weights (downloaded during conversion, not committed)
scripts/nima_weights/
scripts/aesthetic_weights.pth

# Models (distributed via GitHub Releases, not committed)
/models/
*.mlpackage.zip

# Xcode
*.xcworkspace/xcuserdata/
xcuserdata/
DerivedData/
*.xcuserstate
*.xcbkptlist

# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/

# Test assets (large binary files, not committed)
testing/
!testing/bench/
!testing/bench/**

# Re-exclude artifacts underneath the bench whitelist
testing/bench/**/__pycache__/
testing/bench/**/*.pyc
testing/bench/**/.venv/
testing/bench/**/.cache/
testing/bench/**/data/
testing/bench/**/results/

# Tooling
.superpowers/
.claude/

# Git worktrees
.worktrees/

# ML model binaries (large, distribute via releases not git)
*.mlmodelc/
# Ignore every entry inside ImageRater/MLModels/ except the committed MUSIQ-AVA
# package. Git cannot re-include files below a fully-ignored directory, so we
# use /* here (matches entries) rather than /  (matches directory itself).
ImageRater/MLModels/*
# Two negation lines required: the first un-ignores the .mlpackage directory
# entry itself so git descends into it; the second un-ignores every file
# inside it.  Either line alone leaves the package partially ignored.
!ImageRater/MLModels/musiq-ava.mlpackage
!ImageRater/MLModels/musiq-ava.mlpackage/**

